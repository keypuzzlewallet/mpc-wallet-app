enum RequestTransactionType {
  SEND, // Send native fund from one address to another address
  SEND_TOKEN, // Send token from one address to another address
  ETH_SMART_CONTRACT_CALL // Ethereum like smart contract transaction
}

