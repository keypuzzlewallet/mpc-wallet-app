import 'package:mobileapp/models/action.dart';

// all available wallets will be reloaded from db
class ReloadSigningList extends Action {
  ReloadSigningList();

  @override
  String toString() {
    return "[]";
  }
}
